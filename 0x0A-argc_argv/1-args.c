#include "main.h"
#include <stdio.h>
/**
 * main - Programme entry points
 * @argc: number of arguments
 * @argv: Arguments array
 * @i: iteration
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc - 1);
return (0);
}
