#include "main.h"
#include <stdio.h>
/**
 * main - Programme entry point
 * @argc: Number of arguments
 * @argv: Arguments array
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
printf("%d. %s\n", argc, argv[0]);
return (0);
}
