#include "main.h"
/**
 * main - Test function for positive or negative
 * positive_negative - checks if integer is negative or positive
 * Return: Always 0
 */

int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
