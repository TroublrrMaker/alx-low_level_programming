#include "main.h"
/**
 * print_square - Function prints the size of a square
 * Return: Always 0
 */

void print_square(int size)
if (size <= 0)
{
{
_putchar('\n');
}

else
{
int a, b;
for (a = 1; a <= size; a++)
{
for (b = 1; b <= size; j++)
_putchar('#');
}
}
}

