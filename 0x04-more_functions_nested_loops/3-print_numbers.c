#include "main.h"
/**
 * print_numbers- function is used to print the digit; 0 to 9
 * Return: the program has no return it is void
 */
void print_numbers(void);
{
int i;

if (i = 48; i < 58; i++)
{
_putchar(i);
}
_putchar('\n');
}
