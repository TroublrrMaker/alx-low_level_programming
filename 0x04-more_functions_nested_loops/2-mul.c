#include "main.h"
/**
 * mul - function is used to multiply two integers
 * @a : is the first integer to be multiplied
 * @b : is the second integer to be worked on
 * Return: the program returns mul
 */
int mul(int a, int b)
{
int res;
res  = a * b;
return (re);
}
